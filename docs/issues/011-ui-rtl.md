# Issue 011 — Minimal RTL web UI + sessions (FR-7)

- **Type:** feature · **Milestone:** M3 · **Status:** open
- **Scope:** Minimal functional web UI: login, ingest (upload + status), ask with citations,
  run screening workflow with live progress, act on the approval gate, view a trace + usage.
  `dir="rtl"` toggling and correct bidi handling (T1). Persistent chat session history.
  Plain and functional; no visual design time.
- **Why:** FR-7 and T1 RTL rendering.
- **Acceptance:** every core capability reachable from the UI; Arabic view renders RTL
  correctly; sessions survive reloads.